# Commenting Guidelines

This repository is written in Rust. We use Rust's native documentation format (rustdoc) instead of JSDoc/Sphinx.

- Main guide (Chinese): `docs/注释规范.md`
- Coverage report: `docs/doc-coverage-report.md` (generated by `scripts/doc-coverage.ps1`)

